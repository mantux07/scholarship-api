# Author: Tim Smith
# Note: All Code owned by Tim
#
# Python dependencies for Scholarship Research API

Flask==3.0.0
Flask-CORS==4.0.0
openpyxl==3.1.2
reportlab==4.0.7
gunicorn==21.2.0

# Note: Web scraping dependencies (beautifulsoup4, requests, lxml) are NOT needed
# for the Flask API. They are only used by scholarship_updater.py which runs on
# GitHub Actions. The API only reads the pre-generated scholarship_database.json file.
